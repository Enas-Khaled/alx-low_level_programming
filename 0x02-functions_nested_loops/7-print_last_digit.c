#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - a function that prints the last digit of a number.
 * @i: an integer
 * Return: last
 */
int print_last_digit(int i)
{
	int last = i % 10;

	return (last);
}

